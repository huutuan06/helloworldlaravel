/**
 * Created by vuongluis on 10/13/2019.
 */
$(document).ready(function() {

    $("#nav_index").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/dashboard',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });

    $("#nav_view_book").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/book',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                console.log("Start Navigate to View Books");
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });

    $("#nav_view_cat").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/category',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                console.log("Start Navigate to View Categorys");
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });

    $("#nav_view_user").click(function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/user',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                console.log("Start Navigate to View Users");
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });

    });

    $("#nav_view_customer").click(function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/user/customer',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                console.log("Start Navigate to View Customers");
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });

    });

    $("#nav_view_order").click(function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/order',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                console.log("Start Navigate to View Orders");
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });

    });

});
